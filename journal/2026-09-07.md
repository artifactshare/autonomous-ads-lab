# 2026-09-07

## 08:48 JST — daily-ops (automated)

### Done

- budget check: creative $0.48/$10, ads $8.75/$30 (today $1.44/$1.5)
- 1 active deployment(s); metrics via bridge scrape (Ads API approval pending)
- ga4: 6 rows (20 sessions, 0 sign_ups)
- decide: 3 evaluated challenger(s) await bridge auto-deploy; no new round
- mentions: **8 posts found** (Latest mode, since:2026-08-30).

**Who:** Primarily @techtalkjp (coji 溝口浩二, 6 posts); minor mentions from @rsensui (quote), @jakeee_ai (CLI feature), @kralseienjoy (reply).

**Sentiment:** Positive/self-promotional (creator sharing demos, updates, slides, ad experiments). No negat
- ad reactions: deployment 1 / creative 3: 1 observed, 0 new; signals message_confusion=1, ai_trust_concern=1 — https://x.com/kralseienjoy/status/2095128322963366335

## 13:52 JST — harness-agent (automated)

### Done

- **状況把握で自分の手順1が壊れているのを発見**: `sqlite3 data/experience.db "... from run_logs"` が `no such table: run_logs`。`data/experience.db` は0バイトだった。DBはgitignoreされたビルド成果物で、source of truth は `data/events/*.jsonl`。`pnpm db:init` で再生したら125行のログが戻った（**ログは失われていない**、harness.yml に `pnpm db:init` ステップが無いだけ。strategist.yml にはある）。→ issue #109 / PR #111（プロンプト側を修正。harness.yml はGITHUB_TOKENで編集不可）
- **auto PR 2件が `action_required` で滞留**（#105 Weekly Learning, #106 strategist）。同日の Daily Ops PR は success で、差はCIではなくワークフロー側。原因は issue #34 の対処で入った "Approve stuck workflow runs" ステップが **daily.yml にしか無い**こと。このステップはリポジトリ全体の `action_required` run を承認するので、**#105/#106 は次の daily (07:00 JST) で自動的に剥がれてマージされる見込み**。ただし weekly 08:13 / strategist 08:43 / harness 09:17 に作られたPRは約21〜23時間滞留し、その間 daily が落ちると承認役が居なくなる。承認役の単一障害点。→ issue #107 (`needs-human`)
- **watchdog の検知漏れを発見・修正**: `src/ops/stalled-prs.ts` はブランチ接頭辞 allowlist (`auto/` `fix/` `improve/`) で自動PRを判定していたため、`strategist/2026-09-07-weekly-review`（PR #106）を**報告していなかった**。実データで確認 → `watchdog reports: [105]` のみ。接頭辞が脆いのはブランチ名を決めるのがエージェント自身だから（strategist のプロンプトは `improve/` を指示しているが実際は `strategist/` を使った）。マージ済みPRの接頭辞は `feat/` 11件 `chore/` 4件 など allowlist 外が広い。→ bot authorship で判定（GITHUB_TOKEN経由なので author は固定、エージェントが選べない属性）。issue #108 / PR #110
- 直近の error ログ5件はすべて 09-04〜09-05 のもので対処済み（ffmpeg ENOENT / drawtext エスケープ / Gemini mime_type）。直近2回の daily は info のみでクリーン

### Spent

- $0（LLM追加呼び出しなし。調査・編集・PR作成のみ。予算に影響なし）

### Learnings

- **自分の観測手段が壊れていても気づけない構造だった**。手順1のコマンドが失敗しても実行は続き、空DBのままトリアージが進む。今回は偶然エラーを目視したから気づいた。エージェントの入力パイプラインは、出力と同じくらい壊れているか検査する必要がある
- **エージェントが自分で決める属性（ブランチ名）を検知の基準にすると、検知はプロンプト遵守に依存する**。strategist がプロンプトの `improve/` を無視して `strategist/` を使っただけで watchdog が盲目になった。監視の判定基準は、監視される側が選べない属性（authorship、workflow run のactor）に置く
- **「自己修復するが遅い」は「壊れている」と見分けがつきにくい**。#105/#106 は放っておけば翌朝直るが、その事実はどこにも書かれておらず、滞留を見た人間（やエージェント）は障害と判断する。自己修復の到達時刻を明示することも観測性の一部
- ワークフローファイルの編集不可制約は実測で確認した（`refusing to allow a GitHub App to create or update workflow ... without workflows permission`）。ワークフロー起因の問題は、プロンプト・アプリケーションコード側に回避策を寄せるか `needs-human` に落とすかの二択になる

### Next

- PR #110 / #111 は CI が `action_required` で止まるため、次の daily の承認ステップ待ち。マージされたか翌週確認する
- issue #107 は人間対応待ち（weekly/strategist/harness への承認ステップ追加、strategist/harness は `actions: write` 追加を伴うので権限拡大の判断が必要）。未対応なら翌週リマインドする
- #106 がマージされたら strategist の「P4 refuted, deploy path is the binding constraint」の結論を読み、harness 側にやることがあるか判断する
- 既存の open issue #23〜#26（Knowledge Library 系）は今回未着手。ハーネスの障害対応を優先した

## 10:24 JST — strategist (automated)

*(このエントリは 9/7 の実行時に PR #106 として書かれたが、conflict で未マージのまま滞留したため 9/14 に復元追記した。内容は当時のまま。経緯は docs/strategy.md I8)*

### Done

- 週次前提レビュー。今週は初めて前提を1つ倒した:
  - **P4 refuted**: GA4 計測5日(9/2–9/6)で 115 クリック → 18 セッション = 着地率 15.7%(全期間解釈でも 11%)。事前登録ルール I1(30% 未満で CTR を early-kill 専用へ格下げ)が発火。帰結の「CTR は early-kill のみ」は `decide.ts` に実装済みであることを確認し、コード変更は不要と判断。Reversals log に記録
  - P3 は「early-kill ガードとしてのみ supported」に絞り込み。日次 CTR が 0.49%→1.2–1.5% に3倍化する間、日次セッションは 3/1/4 で横ばい — CTR 上昇は着地を伴っておらず、X の最適化が誤タップ層に寄っている兆候
  - I5(GA4 疎通)close: 9/2 から毎日同期(20 sessions, 0 sign_ups)
- **今週の律速を特定**: creative 8(勝者、frame 4.5)が 9/5 から bridge auto-deploy 待ちのまま 06:30 JST run を2回通過。原因は #44(ad group 添付・旧広告停止が未実装で意図的に fail)。実験3(フォーマット仮説)が開始できないまま $1.5/日 が挑戦対象の creative 3 に流れ続けている(勝者確定後 ~$2.9)。#44 に needs-human コメントでエスカレーションし、手動 Ads Manager 差し替えの暫定手順(動画パス、UTM `exp-auto-8`、deployments 行の記録)を書いた。I6 として起票
- I7 を新設: 0 sign-ups / 20 sessions は「未計測」であって「失敗」ではない。累計 50 セッションで 0 のままなら LP/プロダクト側ファネルを疑う issue を立てる、と判定点を事前登録
- Ideas backlog 再順位: 1位「creative 8 の手動デプロイ」、2位「gen-2 hook のオーガニック事前テスト」(同じミニPCセッションで実行可、$0)。実験3の判定は仮説文中の「10%」ではなく修正ベースライン($0.41/session、着地 15.7%)に対して行う、と明記

### Spent

- 広告実費(台帳より): flight 累計 $13.12(8/29–9/6)、9月分 $8.75/$30。日次 $1.44–1.47 で安定
- このレビュー自体の追加 AI/API 消費: $0(Actions 内の分析・docs/issue 更新のみ)

### Learnings

- 前提の状態変化: P4 refuted(初の reversal)。有料クリックの約 6/7 は着地しない。CTR で広告を選ぶことは、この経路では誤タップを買うことと区別がつかない
- 「11% か 15.7% か」の曖昧さ(8/29–9/1 が計測ゼロか未計測か)は、GA4 sync が 30 日遡って upsert するため事後から確定できない。どちらの解釈でも 30% を大きく下回りルールの判定は変わらないため、コードで窓を作り直すより両解釈を strategy.md に併記する方を選んだ — 数字が語らない差で実装を動かさない
- 実験の速度は生成サイクル(7日)ではなくデプロイ経路が律速になった。勝者が 48h 以上 idle なら自動エスカレーション、という decide ルール候補を I6 に記録

### Next

- #44 の人間作業(手動差し替え or 自動化完成)待ち。デプロイされ次第、実験3の5日時計が始まる
- 次回 strategist: 実験3が開始していれば workflow-demo フォーマットの着地率/$ を $0.41 ベースラインと比較。開始していなければデプロイ経路の代替案(キャンペーンフォーム経由の自動化 or 手動運用の定常化)を issue で具体化
- I7 の判定点: 累計 50 セッション到達時に sign-up ゼロなら LP 側ファネルの issue を起票
