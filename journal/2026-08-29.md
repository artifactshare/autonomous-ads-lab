# 2026-08-29 — Project kickoff

## Done

- Repo created: [artifactshare/autonomous-ads-lab](https://github.com/artifactshare/autonomous-ads-lab). Handover(v0計画)確定: Human Gateなし、予算のみコードレベルhard constraint、v0は縦一本
- ブロッカー調査: X Ads APIは承認まで数週間〜数ヶ月・応答なしが常態と判明 → 初回入稿は手動(Ads Manager)を本線に変更、APIは承認後に自動化
- X developer account作成(@artifactshare_)、App ID発行、**Ads API Access Form送信完了**(承認待ち)
- X広告アカウント確認: Ads Manager操作可能
- fal.ai: APIキー発行、クレジット$10チャージ
- Phase 1実装: DB schema(Experience DB) / Budget Controller(月次$10+$30+$10、日次ads $1.5、idempotency) / 構造化ログ(JSONL, decision+reason) / GitHub Actions(CI + daily/weekly骨格)。テスト8本、CIグリーン
- Phase 2着手: `VideoGenerator`抽象 + fal H3 Max backend。**初動画の生成に成功**(5秒768p、生成7.4秒、$0.40)
- 評価器の認証: Claude Agent SDK + subscription OAuthで疎通確認

## Spent

- fal: $1.60 / creative予算$10(月) — 内$1.20は下記バグによる重複生成
- X ads: $0

## Learnings

1. **リトライは再送信ではなく結果回収にする。** falのポーリングURLを間違えて405になり、リトライループが送信成功済みのジョブを4回重複生成した($0.40→$1.60)。送信直後にrequest_idを永続化し、失敗時はそのidで結果を取りに行く設計に変える
2. **見積もりと実消費の突合が要る。** Budget Ledgerはauthorize時の見積もり記録なので、失敗・重複時に実態とずれる。reconciliation処理をPhase 2に足す
3. falはカード登録だけでは使えず事前チャージ式。残高反映には数分ラグがある
4. pnpm 11+はビルド許可の設定名が`allowBuilds`に変更されていた(旧`onlyBuiltDependencies`)

## Next

- 評価器(Agent SDKでVLM採点、多軸スコア+hard constraints+pairwise)
- creative pipelineのDB結線(request_id永続化を含む)
- X Ads API承認待ち。1週間応答なければdevcommunityへ催促
- 人間側TODO: X広告アカウントの支払い方法登録
