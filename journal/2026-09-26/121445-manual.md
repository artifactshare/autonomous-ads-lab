## 12:14 JST — manual

### Done

- **auto-merge 停止の根治**: ジャーナルを 1 エントリ 1 ファイル (`journal/YYYY-MM-DD/HHMMSS-<actor>.md`) にした。同じ日のファイルに daily / weekly / strategist / harness が別々の PR から追記していたのが、GitHub 上で衝突する唯一の原因だった (GitHub のサーバー側 merge は `merge=union` を使わない)。これで自動 PR 同士が同じパスを触らなくなる
- `appendJournal` はエントリごとにファイルを作る。人とエージェントは `pnpm journal:new "<actor>"` でファイルを作り、`pnpm journal:show YYYY-MM-DD` で 1 日分を通して読む。living report のカレンダーも旧形式の日ファイルと新形式のエントリを結合して表示する
- merge テストは `.gitattributes` なしの素の git merge で検証するようにした。同じ日のエントリがまた同じパスを共有したら落ちる
- 9/26 以前の `journal/YYYY-MM-DD.md` はそのまま残す (履歴。新規追記はしない)

### Spent

- $0
