## 14:06 JST — manual (owner decision)

### Done

- **有料広告を停止 (オーナー判断)**。10/1 の予算ガードによる自動再開をしない。deployment 2 (creative 8) と 4 (creative 29) を `paused` → `stopped` にした (X 側 line item `xirso` は 9/23 から PAUSED のまま)
- `config.budget.paidMediaEnabled = false` を追加。入稿 (`deployCreative` の apply)、手動再開 (`ads:control` の resume)、予算ガードの自動再開がすべてこれで止まる。CI の budget-guard が false を固定するので、エージェントは戻せない
- 理由: 月 $30 では LP 着地が月約 34 人。サイト全体の登録率は約 1% なので、期待値は月 0.3 件で、この予算では Goal に答えが出ない。9 月は実支出約 $33、有料の初回ユーザー 34 人、sign_up_start 0、sign_up 0。GA4 の sign_up 計測自体は正常 (#171 にコメント済み)
- 9 月の登録の約 8 割は、共有された成果物を見るためにログインした人。strategist は今後、有料チャネルの案を parked として扱い、共有成果物の閲覧者と困りごとへの返信に労力を向ける (docs/strategy.md 冒頭に記載)

### Spent

- $0

### Next

- 再開するなら人間が config と CI の期待値を両方変える。判定するなら月 $300 規模が必要という試算
