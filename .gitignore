.env
.env.*
.DS_Store
node_modules/
data/*.db-wal
data/*.db-shm
logs/
data/*.mp4
data/creatives/*/raw.mp4
data/creatives/*/frames/
data/testeval-frames/

# Experience DB is a build artifact rebuilt from data/events/
data/experience.db
data/experience.db-wal
data/experience.db-shm

# Living report is a build artifact rendered from the DB and uploaded to
# artifactshare; nothing reads it back from the repo. Tracking it made daily
# and weekly conflict every time both ran on the same day (see #133).
data/living-report.html
