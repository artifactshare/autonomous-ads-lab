# Base footage: the review loop, recorded 2026-09-14

Real recordings (no generated video): `comment.mp4` (Playwright, a teammate selects the title and leaves a note on a shared Artifact Share page), `terminal.mp4` (Cap, Claude Code reads the note, edits, republishes), `reveal.mp4` (Playwright, same URL now shows the new title and the resolved thread). Font LINE Seed JP (OFL). How they were made: private skill `loop-demo-video`.

Creatives in footage mode reuse these clips and only change the hook card, captions and end card (see `src/creative/footage.ts`).
